[build]
builder = "nixpacks"

[deploy]
healthcheckPath = "/api/herbs"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production"
PORT = "3000"
NPM_CONFIG_LEGACY_PEER_DEPS = "true"